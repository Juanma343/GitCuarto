\begin{lstlisting}
int i = defenses.size()-1;
while(i > 1){
    if ((tabla[i][cost] != tabla[i-1][cost])){

        cost -= coste[i];
        selectedIDs.push_back(id[i]);
    }
    i--;
}
\end{lstlisting}